# P2P Remote LAN native v2

Native v2 ???? **GStreamer RTP/H.264** ??????????? UDP ?????

```text
macOS Host:    GStreamer avfvideosrc -> vtenc_h264_hw -> rtph264pay -> UDP 45000
Windows Client: GStreamer udpsrc -> rtpjitterbuffer drop-on-latency -> d3d11videosink
Input:         Windows UDP binary input packets -> macOS CGEvent injection on UDP 45001
```

## ??

macOS?

```bash
brew install gstreamer gst-plugins-base gst-plugins-good gst-plugins-bad gst-plugins-ugly
```

Windows??? GStreamer MSVC x86_64 runtime + development package????????

```powershell
$env:GSTREAMER_1_0_ROOT_MSVC_X86_64 = 'C:\gstreamer.0\msvc_x86_64'
```

`native-v2/win-client/build.ps1` ???? GStreamer `bin` ? `pkgconfig` ???????? CMake ??? GStreamer development files????????????

## 1) ?? macOS Host

???macOS 13+?Xcode Command Line Tools?GStreamer?

```bash
cd native-v2/mac-host
swift build -c release
```

???

```bash
.build/release/p2p-native-mac-host   --client-ip 192.168.1.50   --video-port 45000   --input-port 45001   --width 1920   --height 1080   --fps 60   --bitrate 18000000   --keyint 1   --transport udp
```

macOS ???

- Privacy & Security -> Screen Recording?????????? Host App?
- Privacy & Security -> Accessibility?????????? Host App?

## 2) ?? Windows Client

???Windows 10/11?Visual Studio 2022 Build Tools?CMake?GStreamer MSVC x86_64 development package?

```powershell
cd native-v2\win-client
.uild.ps1
```

???

```powershell
.uild\Release\p2p-native-win-client.exe `
  --host-ip 192.168.1.20 `
  --video-port 45000 `
  --input-port 45001 `
  --width 1920 `
  --height 1080 `
  --fps 60 `
  --transport udp `
  --fullscreen
```

???? ultra ???

```powershell
.un-ultra.ps1 -HostIp 192.168.1.20 -Width 1920 -Height 1080 -Fps 60
```

????? RTP/UDP 45000???? UDP 45001?Windows ???????????????????????? `p2p-native-win-client.exe` ?? UDP 45000?

## ?????

- GStreamer `rtpjitterbuffer latency=10 drop-on-latency=true`???????????
- ??/??? `queue max-size-buffers=1 leaky=downstream`???????????
- macOS ?? `vtenc_h264_hw realtime=true allow-frame-reordering=false`?
- Windows ?? `d3d11videosink sync=false async=false`????????????
- ????? 32 ?????? UDP?

## ????

- ?? GStreamer ??? UDP/RTP ????????? TCP ?????
- Windows ?????? GStreamer sink ???? HUD/???????????
- ???/??/???????? Host pipeline ????
